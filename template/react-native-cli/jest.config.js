module.exports = {
  // Use the official Jest preset for React Native.
  // This preset configures Jest with sensible defaults for RN projects:
  // - Handles Babel transformation of modern JS/TS + JSX
  // - Provides mocks for React Native modules (e.g. Image, StyleSheet)
  // - Configures environment to simulate a mobile-like runtime
  preset: 'react-native',
};
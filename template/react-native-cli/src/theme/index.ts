import { theme } from './utils/theme'
import { useTheme } from './useTheme'
import { createStyles } from './createStyles'

import type { Theme, Color, ThemeType } from './utils/theme'

export { type Theme, type ThemeType, type Color, theme, useTheme, createStyles }
